// Database mixes multiple values for gender, so we need to map them to a single value.
// It would be great if database would be consistent, but it is not.
export const databaseMaleGenderValues: string[] = ['male', 'm', 'M'];
export const databaseFemaleGenderValues: string[] = ['female', 'f', 'F'];
